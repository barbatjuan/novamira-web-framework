# `STY-DIRECT` — Direct

> Ported from `design-personalities.md`'s `PERS-DIRECT` (style-catalog PR 4a, `tasks.md` 4a.2).
> Format only — the 8 axis positions and every prose claim below are unchanged from the source; see
> `_README.md` for what a port means and what still lives in the old file until PR 4c.

**Axes:** scale `monumental` · ground `ink` · density `compact` · composition `broken-grid` ·
elevation `accent-glow` · accent `gradient` · chassis `bare` · ornament `none`

**Fits:** Brands that win by being unmistakable — studios, launches, anything that must not read
as safe.

**Typography:** `--font-primary` **Archivo Expanded** at 700; `--font-secondary` **Archivo**
regular. One family, two extremes. SIL OFL. Display tracking `-0.025em` (h1–h3), wordmark `.1em`.

**Motion intensity:** short durations, confident lift, accent glow rather than a neutral shadow.

**Imagery:** high-contrast, tightly cropped, often bleeding past the grid.

**Card recipe:** dark surface, accent glow border on hover instead of lift.

## Toggle precharge

| Toggle | Precharge | Why |
|---|---|---|
| `TGL-IMAGERY` | `foto` | "high-contrast, tightly cropped" is a claim about real campaign photography |
| `TGL-MOTION-INTENSITY` | `audaz` | "short durations, confident lift" is the bold end, not the calm one |
| `TGL-HERO-TYPE` | `imagen fija` | one striking bled image beats a slider's diluted attention — a brand that "must not read as safe" does not hedge across three frames |
| `TGL-CTA-STRENGTH` | `fuerte` | winning by being unmistakable is a claim about the CTA too |
| `TGL-CARD-STYLE` | `compacta con datos` | `compact` density; the card recipe reads closer to a spec sheet than a lifestyle spread |
