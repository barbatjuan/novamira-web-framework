# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Never commit site secrets or connector tokens
*.env
.env*
*connector*token*
secrets.*

# Auto-generated local tool state from `gentle-ai skill-registry refresh`; embeds absolute
# developer paths, so it is machine-local, not repo content.
.atl/
